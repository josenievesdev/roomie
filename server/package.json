{
  "name": "roomie-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Servidor multijugador de Roomie (Node + Socket.io, AvatarState autoritativo)",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "start": "node src/index.ts"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
